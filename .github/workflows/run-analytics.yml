name: Run Code Review Analytics

on:
  workflow_dispatch:

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run analytics action
        uses: ./.github/actions/analyze
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
